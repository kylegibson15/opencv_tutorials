cmake_minimum_required(VERSION 2.8)
project(BlendingImages)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(BlendingImages BlendingImages.cpp)
target_link_libraries(BlendingImages ${OpenCV_LIBS})
